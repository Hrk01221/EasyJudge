To Activate virtual environment(Production level)
# python -m venv venv
# venv/Scripts/activate
# pip install -r requirements.txt
# pip install fastapi uvicorn
# pip install "fastapi[standard]"

To get out of venv
. deactivate

# Usefull extension
. thunder client
. SQLite3 Editor

# install jwt
. pip install "python-jose" "passlib[bcrypt]" "python-multipart"


# install dotenv
. pip install python-dotenv py

# install pytest
. pip install pytest

# install requests
. pip install requests

# install for web crawling
. pip install beautifulsoup4

# install for rate limiting
. pip install slowapi

# for postgresql db
. pip install psycopg2-binary

# Deployment
. pip freeze > requirements.txt
. uvicorn <main_file_name>:app --host 0.0.0.0 --port 8000
. .gitignore create